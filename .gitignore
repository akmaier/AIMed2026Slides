# --- Source PowerPoint and derived bulk assets (not for the public repo) ---
# We DO track the final (manually-edited) AIMed2026_What_next_in_medical_AI.pptx
# at the repo root; everything else .pptx-shaped stays local:
#   * the original Besonderer Vortrag Maier.pptx (private source);
#   * Office lock files like ~$AIMed2026_*.pptx;
#   * regenerated build_pptx/*.pptx artefacts from build_pptx.py.
Besonderer Vortrag Maier.pptx
~$*.pptx
build_pptx/*.pptx
build_pptx/pages/
build_pptx/verify/
slide_images/
extracted_pptx/

# --- macOS / editor noise ---
.DS_Store
*~
*.swp

# --- LaTeX build artefacts ---
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.nav
*.out
*.run.xml
*.snm
*.synctex.gz
*.synctex(busy)
*.toc
*.xdv

# --- Claude Code worktrees / local agent state ---
.claude/

# --- Local TeX vendor copies (kept under output_tex/, regeneratable) ---
faubeamer_newdesign-main/

# --- PowerPoint analysis helper output (regenerable from the pptx) ---
analyze_layout.py
check_timing.py
extract_pptx.py
inspect_clicks.py
